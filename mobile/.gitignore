node_modules/
.expo/
dist/
web-build/
android/
ios/
*.log
.DS_Store

# Firebase Android configuration. Not a secret in the sense an API key is
# not a secret, but it is per-project and generated from the console rather
# than authored here, so it is fetched at build time instead of committed.
google-services.json

# Design harness output. Regenerate with `node preview/build.mjs`.
preview/out/
preview/shots/

# Play Store graphics, regenerated with `npm run store-graphics`.
store/
